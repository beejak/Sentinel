offline: false
policy:
  enable_private_egress_checks: false
http:
  verify: true
  # cert:
  # key:
  # proxy: http://127.0.0.1:8080
  timeout: 10
  headers:
    User-Agent: "sentinel/0.1 (+https://example.invalid)"
    Accept: "application/json, */*"
    # Authorization: "Bearer <TOKEN>"
    # X-API-Key: "<KEY>"
    # Proxy-Authorization: "Basic <BASE64_USER_PASS>"
    # X-Request-ID: "<RUN_ID>"

# Per-domain overrides (only sent when host matches). Examples:
# domains:
#   api.example.com:
#     headers:
#       Authorization: "Bearer <TOKEN>"
#   .internal.example.com:
#     headers:
#       X-Org: "security-scan"
